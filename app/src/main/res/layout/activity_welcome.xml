<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/welcome"
    android:padding="16dp"
    tools:context="com.nexis.running.activitys.WelcomeActivity">


    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="Activity Tracking App"
        android:textColor="@android:color/black"
        android:textSize="24sp" />

    <Button
        android:id="@+id/welcome_CreateAccount_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textView"
        android:layout_marginTop="478dp"
        android:background="@color/purple_200"
        android:radius="10dp"
        android:text="Create an account"
        android:textColor="#ffffff" />

    <Button
        android:id="@+id/welcome_login_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/welcome_CreateAccount_button"
        android:layout_marginTop="18dp"
        android:background="@color/purple_200"
        android:radius="10dp"
        android:text="Login to my account"
        android:textColor="#ffffff" />
</RelativeLayout>